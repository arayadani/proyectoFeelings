@model List<proyectoFeelings.ViewModels.RecordViewModel>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button"
                class="btn-close"
                data-bs-dismiss="alert">
        </button>
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button"
                class="btn-close"
                data-bs-dismiss="alert">
        </button>
    </div>
}

<table class="table">
    <thead>
        <tr>
            <th>Tipo</th>
            <th>Tienda actual</th>
            <th>Tienda destino</th>
            <th>Codigo</th>
            <th>Descripcion</th>
            <th>Cantidad</th>
            <th>Proveedor</th>
            <th>Fecha de la notificacion</th>
            <th>Comentario</th>



        </tr>
    </thead>
    <tbody>

  @foreach (var record in Model)
        {
            <tr>
                <td>
                    @(record.Type == 1 ? "Traslado" :
                          record.Type == 2 ? "ROP" :
                          "Otro")
                </td>

                <td>
                    @(record.CurrentStoreID == 7 ? "Multicentro" :
                      record.CurrentStoreID == 8 ? "Terramall" :
                      record.CurrentStoreID == 9 ? "San Sebastian" :
                      "Otro")
                </td>
                <td>
                    @(record.NewStoreID == null ? "N/A" : record.NewStoreID)
                </td>
                <td>@(record.Code)</td>
                <td>@(record.Description)</td>
                <td>@(record.Quantity)</td>
                <td>@(record.Provider)</td>
                <td>@(record.DateTime.ToString("dd/MM/yyyy"))</td>
                <td>@(record.Comment)</td>


                @if (record.Type == 1)
                {
                    <td>
                        <a asp-controller="Product"
                           asp-action="ApproveMove"
                           asp-route-ProductID="@record.ProductID"
                           asp-route-StoreID="@record.CurrentStoreID"
                           class="btn btn-success btn-sm">
                            Aprobar
                        </a>
                    </td>
                    <td>
                        <a asp-controller="Product"
                           asp-action="RejectMove"
                           asp-route-ProductID="@record.ProductID"
                           asp-route-StoreID="@record.CurrentStoreID"
                           asp-route-Type="@record.Type"
                           class="btn btn-danger btn-sm">
                            Declinar
                        </a>
                    </td>
                }
                @if (record.Type == 2)
                {
                    <td>
                        <a asp-controller="Product"
                           asp-action="AckNotification"
                           asp-route-ProductID="@record.ProductID"
                           asp-route-StoreID="@record.CurrentStoreID"
                           asp-route-Type="@record.Type"
                           class="btn btn-success btn-sm">
                            Entendido
                        </a>
                    </td>
                    
                }
                
               
            </tr>
        }
    </tbody>
</table>